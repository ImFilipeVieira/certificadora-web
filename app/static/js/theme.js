(function () { const KEY = 'certificadora-theme'; const root = document.documentElement; const media = window.matchMedia('(prefers-color-scheme: dark)'); const labels = { light: '☀️ Claro', dark: '🌙 Escuro', system: '💻 Sistema' }; function saved() { return localStorage.getItem(KEY) || 'system' } function resolved(v) { return v === 'system' ? (media.matches ? 'dark' : 'light') : v } function apply(v) { root.setAttribute('data-theme', resolved(v)); root.setAttribute('data-theme-choice', v); document.querySelectorAll('[data-theme-current]').forEach(e => e.textContent = labels[v] || labels.system); document.querySelectorAll('[data-theme-option]').forEach(b => b.classList.toggle('theme-option-active', b.dataset.themeOption === v)) } function set(v) { localStorage.setItem(KEY, v); apply(v) } apply(saved()); document.addEventListener('DOMContentLoaded', () => { apply(saved()); document.querySelectorAll('[data-theme-option]').forEach(b => b.addEventListener('click', ev => { ev.preventDefault(); ev.stopPropagation(); set(b.dataset.themeOption); const menu = b.closest('details'); if (menu) menu.open = false })); document.addEventListener('click', ev => document.querySelectorAll('details.theme-menu[open]').forEach(m => { if (!m.contains(ev.target)) m.open = false })) }); media.addEventListener('change', () => { if (saved() === 'system') apply('system') }); })();
